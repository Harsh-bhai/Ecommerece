import { useRouter } from 'next/router'

const Slug = () => {
  const router = useRouter()
  const { slug } = router.query

  return <>
    
    <div className="flex">
      yaha likhna
      
    </div>
    
    
    
    </>
}

export default Slug